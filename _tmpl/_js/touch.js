// NOTE: the real contents of this file would actually do something useful.
// for demo purposes, it logs a message to the page.
(function( $ ){
	// when dom is ready...
	$(function(){
		$( "<p>generalenhancements.js was loaded because touch events are supported.</p>" ).appendTo( "body" );
	});
})( wrap );